<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/styles/main.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>new note</title>
</head>
<body class="create">
    <div class="container mt-5 justify-content-md-start align-items-center ">
        <div class="row justify-content-center me-5">
            <div class="mt-5 d-flex justify-content-center align-items-center mb-5">
                <h2>
                    share your review and personal notes on the books that inspired you!
                </h2>
            </div>
            <div class=" col-lg-7 ">
                <div class="card p-4 ">
            
                   
                    <form action="/update/<%=review.id%>" method="post">

                        <input type="hidden" name="updatedId" value="<%= review.id %>">

                        <div class="mb-3">
                            <label for="title" class="form-label">Title</label>
                            <input type="text" class="form-control" id="title" name="title" placeholder="<%= review.book_title %>" readonly>
                        </div>

                        <div class="mb-3">
                            <label for="review" class="form-label">Review</label>
                            <textarea class="form-control" id="review" rows="4" name="updatedReview" placeholder="<%= review.review %>"><%= review.review %></textarea>
                        </div>

                        <div class="rating-creat mb-3"> 
                            <label for="rating" class="form-label">Rating</label>
                            <select class="form-select" id="rating" name="rating" >
                            <option value="1">1 Star</option>
                            <option value="2">2 Stars</option>
                            <option value="3">3 Stars</option>
                            <option value="4">4 Stars</option>
                            <option value="5">5 Stars</option>
                            </select>
                            </div>

                            <div class="mb-3">
                                <label for="date" class="form-label">created at: </label>
                                <input type="text" class="form-control" id="date" name="date" value="<%= date %>"  placeholder="<%= date %>" readonly>
                            </div>


                            <div class="d-flex justify-content-end">
                            <button type="submit" class="btn btn-outline-dark pt-3 pb-3 ps-4 pe-4 rounded-pill mt-3">Save Changes</button>
                            </div>

                    </form>
        </div>

        

    </div>

    <div class="mt-5 d-flex justify-content-center align-items-center mb-5">
        <h2>
           You made a mark that lasts!
        </h2>
    </div>


    </div>
    </div>
    



<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>